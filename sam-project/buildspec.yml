version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.8
  build:
    commands:
      - sam build -t sam-project/template.yaml
      - sam deploy --region us-west-1 --stack-name serverlessApp --no-confirm-changeset --no-fail-on-empty-changeset
